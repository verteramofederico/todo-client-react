import TodoAdd from './TodoAdd'
import TodoContainer from './TodoContainer'

function Todo(props) {
    return (
        <>
        <TodoAdd/>
        <TodoContainer/>
        </>
        )
}

export default Todo;